我要部署“学生作品展示”网站。请你全程一步一步带我完成，不要一次告诉我很多步骤。最低使用 GPT-5.6 Sol，思考程度使用“高”；遇到部署失败、资源绑定、数据库迁移、账号串号或复杂故障时改为“超高”。

开始前先确认：1）我是否能正常登录 ChatGPT、GitHub、Cloudflare；2）当前浏览器里登录的是谁的 GitHub；3）当前浏览器里登录的是谁的 Cloudflare；4）这是这个 Cloudflare 账号里的第几个作品集网站；5）如果同一账号已经有网站，必须为这次网站使用新的仓库名、Worker 名、D1 和 MEDIA_KV，不能复用旧网站资源。

如果同一个 GPT 账号正在帮助不同学生部署，必须以当前 GitHub 和 Cloudflare 的实际登录身份为准。发现浏览器还登录着上一位学生的 GitHub 或 Cloudflare，就先让我退出并切换账号，确认正确后才能继续。

确认环境后，再把官方 Deploy to Cloudflare 链接给我，并一次只指导我完成一个页面。可以自动完成的检查、代码、部署、迁移和测试请直接完成；只有官方账号登录、OAuth 授权、一次性部署口令、管理员密码、系统恢复码这些必须本人处理的步骤才叫我操作。

不要向我索取 GitHub 密码、Cloudflare 密码、管理员密码、INITIAL_ADMIN_CODE、系统恢复码、浏览器 Cookie 或长期 API Token，也不要让我把这些内容发到聊天里。

首次部署必须创建独立 Worker、独立 D1 DB、独立 MEDIA_KV。部署完成后继续带我打开 /admin，完成管理员初始化并保存恢复码，然后检查：后台密码门禁、安全退出、网站空间、程序升级中心、图片上传、MP4 播放与拖动、草稿预览、正式发布和二维码状态。遇到报错时先定位已有资源，不要重复创建第二套 Worker、D1 或 KV。