/** Current preparation text; historical release manifests and prompts stay frozen. */
export const UPGRADE_PREPARATION_PROMPT = `请帮助我为自己的现有学生作品网站准备升级到 1.3.1-C，并在准确发布条件与本网站授权都满足后完成同站升级。先完成以下只读核对，向我说明实际缺口；准备指令本身不表示目标已经正式发布，也不授权绕过审核或改动其他网站。

1. 核对目标是否真正可分发。
从官方仓库 q1433031046-ship-it/student-portfolio-cloudflare 核对固定发布标签 v1.3.1-C、标签解析后的准确提交与 Tree、配套 deployment/agent-manifest.json、template-version.json、upgrade-prompt.json、prompt 的 SHA-256 和适用正式审核记录。当前 1.3.1-C 仍标记 unreleased、releaseTag=null，历史清单与 prompt 对应 v1.3.0；某个网站运行 C 不能证明通用模板已经发布。标签缺失、清单仍为历史版本、摘要或审核对象不一致时，只做原站只读盘点，明确停止升级写入。不要用分支最新提交替代固定发布对象，不修改历史清单来制造通过，不自动合 main、打标签或创建 Release。

2. 识别并保护我的原站。
确认当前所有者、GitHub 仓库和分支、Cloudflare 账号、原 Worker、动态域名、原 Pages 项目和固定静态域名。记录原 commit、未提交改动和可恢复工作区；保留已有改动，不强制切换或清空。只读记录当前生产 Version/Deployment、流量、D1 迁移账本、草稿与已发布内容的数量、媒体元数据、DB 和唯一 MEDIA_KV 的精确 ID、必要旧 BUCKET、其他绑定及运行配置的资源指纹。秘密只核名称和类型，不读取或输出值；管理员密码、恢复码、token 不放进聊天。需要本人操作时，只在原账号官方界面或已授权的隐藏输入处完成。

3. 核对产物已适配我的站点。
保留原 Worker、D1、MEDIA_KV、账号、域名、免费套餐、内容、图片、视频和历史数据。原站缺少固定 DB ID 或唯一 MEDIA_KV ID，或实际绑定与配置不一致时停止；纯 R2-only 旧站不能直接套用现有升级入口。当前候选的 app/lib/site-entrances.ts 含本站专用 Pages 地址和上传地址，app/api/admin/static-package/route.ts 含本站专用 adminOrigin，并会写入 ZIP。必须确认准确目标产物已适配我的原站地址与资源；尚未适配就列明缺口并停止升级写入。不要把示例站点的 account、project、URL、资源 ID 或凭据复制给我的网站，也不要仅改说明就宣称适配完成。

4. 核定必要迁移与执行路径。
以目标准确清单、迁移文件摘要和我的真实账本逐项比较。C 的正常迁移涉及 0008–0011；只应用确实缺少且结构与摘要匹配的必要增量，不套用 v1.3.0 旧“止于 0007”说明，不重放已入账迁移，不删除数据。runtime safe bootstrap 只列 0006/0007，不能用打开 /admin 代替 0008–0011 的权限和迁移准备。查询、权限、结构或摘要不符时停止，不自动回退数据库结构。
先读取 README.md、AGENTS.md、准确部署清单及脚本。cloudflare:fingerprint 使用 inspect 路径，只做部署前指纹检查；cloudflare:deploy 会构建、核对指纹、应用 pending D1 migrations 并真实部署，不能当只读命令执行。不要把旧命令当作已通过通用单次执行审核：先核定本次确切请求、自动触发和重试行为、数据影响、一次尝试边界、停止条件及审核要求。保留原站配置和全部必要绑定，隔离工作树只用于准备和核验。

5. 冻结升级与恢复对象后再执行。
仅在正式目标、原站适配、迁移、准确审核和本次授权全部满足后，形成可审核的版本、绑定、部署对象及恢复基线。恢复要覆盖代码和数据兼容性；不要把切回旧 Worker 版本说成能撤销数据库变更。保留原站资源及内容。发生错误、超时或结果不明时，先读取本次实际状态和已用尝试标记，不盲目重发、不重置标记、不自动删除资源或恢复数据库。无需为同范围已给授权重复询问，但秘密亲自输入、扩大权限、付费、删除及其他实际新增事项须单独落实。

6. 按实际版本条件完成管理员确认。
从已确认的 1.3.0 升级到 C，当前实现需要我在原 /admin 使用当前最新恢复码确认、重新设置密码并保存新恢复码；完成恢复后旧码及旧管理员会话失效。已确认的 1.3.1-b 到 1.3.1-C 是精确例外；同 C 再部署不会自动轮换恢复码。不要把例外扩展到其他版本。让我亲自输入恢复码，只报告是否完成，不获取其内容。生成新文件时按页面实际版本核对 {hostname}-v{实际版本}-系统恢复码-{YYYYMMDDTHHMMSSZ}.txt，并让我打开文件确认站点后离线保存。旧 R2 媒体如确需迁移，先独立核定同一 BUCKET、原 ETag、可续跑的逐块校验和完整 KV 回读，不自动删除源对象。

7. 分别更新动态和静态网站。
推荐日常顺序：等待媒体上传完成 → 保存草稿 → 点击实际“发布动态前台 →”并等待“动态前台已更新” → 下载可上传的网站包（ZIP） → 打开自己的原 Cloudflare Pages 上传页 → 核对项目和 Production → 选择整个 ZIP、无需解压 → 全部上传完成后点击 Save and deploy → 看到 Success 后检查自己的固定静态网址。动态发布成功不等于静态更新成功；失败停在对应步骤。ZIP 读取已保存草稿和管理员可读媒体，动态发布只是双站同步的推荐步骤，不是 ZIP 的技术前置。不要自动替我点击发布或重放已经完成的静态部署。

8. 有限验收并交付事实。
核对准确生产 Version/Deployment 与流量、原首页和 /admin 可用、版本显示、草稿和媒体数量、资源指纹及必要迁移。对本次实际变化做有限桌面检查，沿用仍适用的既有证据；不默认开启全套手机优化、重复 ZIP/视频下载、多会话或完整平台测试。未检查的项目如实标明。最后分别报告目标正式发布资格、原站升级、动态内容发布、静态发布的实际结果，以及准确版本/提交/摘要、恢复基线、唯一关键阻断和需要谁处理的事项。`;
